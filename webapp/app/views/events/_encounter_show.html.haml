%table.list
  %tr
    %th=t 'investigator'
    %th=t 'encounter_date'
    %th=t 'description'
    %th=t 'location'
    %th &nbsp

  - for encounter in encounters

    - if encounter.deleted_at
      - style = {:class => 'struck-through'}
    - else
      - style = {}

    - encounter_event_data = encounter.participations_encounter

    %tr
      %td{style}=h(encounter_event_data.user.best_name)
      %td{style}=ld(encounter_event_data.encounter_date)
      %td{style}=h(encounter_event_data.description)
      %td{style}=h(I18n.t("encounter_location_types.#{encounter_event_data.encounter_location_type}"))
      %td
        .tools
          = show_and_edit_event_links(encounter)
