- content_for :title do
  =t 'permission_denied'

= content_tag(:p, reason + ". #{link_to(t('please_exit'), home_path, :style => 'color: white; text-decoration: underline;')}.", :class => 'banner-warning')
