-fields_for(@event, :builder => ExtendedFormBuilder) do |e|
  -core_element_print(:state_manager_id, e, :horiz) do
    %span.print-label=ct(:state_manager, :scope => :perinatal_hep_b_management)
    %span.print-value=h(e.object.state_manager.try(:best_name))
