%fieldset
  %legend Laboratory Information

  - if @event.labs.empty?
    %h3 No lab results have been recorded for this event
  - else
    %table{:class => 'tabular'}
      %tr
        %th Lab name
        %th Lab result
        %th Specimen source
        %th Collection date
        %th Lab test date
        %th Tested at UPHL lab

      - for lab in @event.labs
        - for lab_result in lab.lab_results
          %tr
            %td= lab.secondary_entity.place_temp.name
            %td= lab_result.lab_result_text
            %td= l lab_result.specimen_source
            %td= lab_result.collection_date
            %td= lab_result.lab_test_date
            %td= l lab_result.tested_at_uphl_yn
