%h1 Editing user

= error_messages_for :user

- form_for(@user) do |f|

  = render :partial => 'form', :locals => { :f => f, :user_data => @user }

  = f.submit "Update"
  = link_to 'Show', @user
  = link_to 'Back', users_path
