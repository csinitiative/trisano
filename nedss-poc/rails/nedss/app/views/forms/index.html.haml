
%h1 Listing forms

%table{:style => "border-spacing: 15px 5px"}
  %tr
    %th Name
    %th Description
    %th Disease
    %th Jurisdiction
    %th{:colspan => 4} Actions
  - for form in @forms
    %tr
      %td
        = h form.name
      %td
        = h form.description
      %td
        = h form.disease.disease_name unless form.disease.nil?
      %td
        = h form.jurisdiction.nil? ? "All" : form.jurisdiction.current_place.name
      %td
        = link_to 'Builder', builder_path(form)
      %td
        = link_to 'Show', form
      %td
        = link_to 'Edit', edit_form_path(form)
      %td
        = link_to 'Destroy', form, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New form', new_form_path
