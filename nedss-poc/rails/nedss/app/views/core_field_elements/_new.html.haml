
%div{:id => "new-core-field-form"}

  %h3
    Add Core Field Configuration

  = error_messages_for :core_field_element

  - remote_form_for(core_field_element) do |f|
    
    - if @available_core_fields.empty?
      %b No core fields are available. Use existing core field configurations.
    - else
      = render :partial => 'form', :locals => { :f => f }

    %p
      - unless @available_core_fields.empty?
        = f.submit "Create"
      &nbsp;&nbsp;
      = link_to_function "Close", "$('core-field-builder-mods').down('form').reset(); Effect.BlindUp($('core-field-builder-mods'), { duration: 0.1 })"
