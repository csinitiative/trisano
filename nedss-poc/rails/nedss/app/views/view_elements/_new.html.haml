
%div{:id => "new-view-form"}

  %h3
    Add tab

  = error_messages_for :view_element

  - remote_form_for(view_element) do |f|

    = render :partial => 'form', :locals => { :f => f }

    %p
      = f.submit "Create"
