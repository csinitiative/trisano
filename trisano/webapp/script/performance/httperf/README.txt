Requires httperf to be installed.

Any linux package manager (e.g., yas, yum, apt-get) will have it.

Sample Usage:
./trisano-httperf-script.sh $HOST_NAME $PORT $TEST_FILE

A specific example:
./trisano-httperf-script.sh test.csi.osuosl.org 80 create_simple_cmr
